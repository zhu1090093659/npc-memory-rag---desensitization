# Core dependencies
elasticsearch>=8.0.0,<9.0.0
google-cloud-pubsub>=2.0.0

# Embedding (ModelScope OpenAI-compatible API)
openai>=1.0.0

# Caching
redis>=4.0.0

# Push worker (FastAPI)
fastapi>=0.100.0
uvicorn>=0.20.0

# Monitoring
prometheus_client>=0.15.0

# Env loading
python-dotenv>=1.0.0

# Optional: for local embedding (if not using API)
# transformers>=4.30.0
# torch>=2.0.0
